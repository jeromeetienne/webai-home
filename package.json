{
  "name": "webai-home",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "npm run build --workspace @webai/protocol && npm run build --workspace @webai/volunteer && npm run build --workspace @webai/server && npm run build --workspace @webai/task-client",
    "dev:server": "npm run dev --workspace @webai/server",
    "test": "npm run test --workspaces"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
