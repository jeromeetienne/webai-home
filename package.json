{
  "name": "webai-at-home",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "npm run build --workspace @webai/protocol && npm run build --workspace @webai/gateway && npm run build --workspace @webai/consumer",
    "dev": "npm run dev --workspace @webai/worker",
    "dev:gateway": "npm run dev --workspace @webai/gateway",
    "test": "npm run test --workspaces"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
