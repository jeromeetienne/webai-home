{
  "name": "@webai/task-client",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "sample:formula": "node --import tsx src/cli.ts --type formula 5 | jq",
    "sample:llm": "node --import tsx src/cli.ts --type llm 'What is the capital of France?'",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "tsx src/cli.ts",
    "test": "node --import tsx --test tests/index.test.ts"
  },
  "dependencies": {
    "@webai/protocol": "0.1.0",
    "commander": "^12.1.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/ws": "^8.5.13"
  }
}
