{
  "name": "@webai/worker-webpage",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "vite",
    "start": "vite preview",
		"deploy": "WORKER_BASE_PATH=/worker/ npm run build && gh-pages --repo git@github.com:webai-at-home/worker.git --branch gh-pages --dist dist --nojekyll --message \"Deploy Worker site\"",
    "typecheck": "tsc -p tsconfig.json",
    "test": "npm run typecheck"
  },
  "dependencies": {
    "@huggingface/tokenizers": "^0.1.3",
    "@huggingface/transformers": "^4.2.0",
    "@webai/protocol": "0.1.0",
    "onnxruntime-web": "^1.27.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "gh-pages": "^6.3.0",
    "typescript": "^5.7.2",
    "vite": "^6.4.3"
  }
}
