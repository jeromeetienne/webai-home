{
  "name": "@webai/server",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "vite build && tsc -p tsconfig.json",
    "start": "NODE_ENV=production node dist/cli.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "tsx watch src/cli.ts",
    "test": "node --import tsx --test tests/cli.test.ts"
  },
  "dependencies": {
    "@webai/protocol": "0.1.0",
    "commander": "^12.1.0",
    "ws": "^8.18.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/ws": "^8.5.13",
    "vite": "^6.4.3"
  }
}