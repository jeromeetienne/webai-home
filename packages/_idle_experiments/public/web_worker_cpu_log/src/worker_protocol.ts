///////////////////////////////////////////////////////////////////////////////
///////////////////////////////////////////////////////////////////////////////
//	WorkerReport — the shape of every message the CPU worker posts back
///////////////////////////////////////////////////////////////////////////////
///////////////////////////////////////////////////////////////////////////////

/** One report posted from the CPU benchmark worker to the main thread. */
export type WorkerReport =
	| { kind: 'worker-cpu'; durationMs: number }
	| { kind: 'worker-timer'; actualDelayMs: number; driftMs: number };
